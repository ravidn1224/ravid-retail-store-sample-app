apiVersion: v1
kind: Service
metadata:
  name: {{ include "ui.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "ui.name" . }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
