apiVersion: v1
kind: Service
metadata:
  name: {{ include "cart.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "cart.name" . }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
